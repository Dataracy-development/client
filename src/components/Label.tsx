export default function Label({ children }: { children: React.ReactNode }) {
    return <label className="text-[11px] font-normal text-[#636ae8] bg-[#f2f2fd] px-1.5 py-[1px] rounded-[10px]">{children}</label>;
}
